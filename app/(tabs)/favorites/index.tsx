import { View, Text } from "react-native";
import React from "react";
import { defaultStyles } from "@/styles";

const index = () => {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>index</Text>
    </View>
  );
};

export default index;
